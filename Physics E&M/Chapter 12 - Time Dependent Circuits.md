#Phys-EM 

- an inductor is a component with non-negligible self-inductance
- So we have to use Faraday's law for circuit analysis now

$\oint\vec{E}d\vec{r}=-\frac{d}{dt}\int\vec{B}\cdot d\vec{S}=-\frac{d}{dt}\int(\vec{B}_{self}+\vec{B}_{ext})\cdot d\vec{S}=-L\frac{di}{dt}-\frac{d}{dt}\int\vec{B}_{ext}\cdot d\vec{S}\neq0$

- The electric field gives positive work on a charge moving across the *inductor*


**Now the loop lap is Faraday's Law lol**
- $\Sigma \epsilon=0$ the sum of the emfs (electromotive forces) is 0
- $\oint \vec{E}\cdot d\vec{r}=-\frac{d\phi_{B}}{dt}=-\frac{d}{dt}\int\vec{B}\cdot d\vec{S}$ *This is the one we're using*
	- We can no longer use $\oint\vec{E}\cdot d\vec{r}=0$

