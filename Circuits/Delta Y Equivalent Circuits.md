When we see [[Circuits]] in a "triangular" resistor configuration (Either "Delta" or "Pi"), we cannot easily simplify to find an [[Equivalent Resistance]].
![[delta_and_pi.png]]

However, we can transform these [[Circuits]] to a Y or T configuration to allow us to simplify easily.
![[Y-T_configuration.png]]

By using these conversions:

![[delta-Y_conversion_equations.png]]

These are derived from the fact that, for the Delta and the Y configurations to be equivalent, then the resistance between nodes must also be equivalent. This gives us these equations:

![[delta_y_terminal_resistance_equivelance_equations.png]]

