An [[Unsupervised Learning]] algorithm (no labels, just data)
- Can provide insight when we have no preconception of the data
- Basic idea is to **group together similar instances**
	- Example: use total squared Euclidian distance as similarity metric for 2D point patterns
