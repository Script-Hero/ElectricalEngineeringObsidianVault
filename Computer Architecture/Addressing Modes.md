1. **Register addressing** where the operand is a register
2. **Base or displacement addressing** where the operand is at the memory location whose address is the sum of a register and an address in the instruction
3. **Immediate addressing** where the operand is a constant within the instruction itself
4. **PC-relative addressing** where teh address is the sum of the PC (program counter) and a constant in the instruction