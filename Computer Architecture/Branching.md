[[B-Type Instructions]] and [[CB-Type Instructions]], allows you to do "goto" statements.

Branch **target address** is represented in PC-relative mode:
- Based on where currently the branch instruction is and *shows the distance it branches over*, rather than the absolute address of the branch target address
- In terms of *# of instructions*
- To branch forward we add a positive number
- To branch backward we add a negative number