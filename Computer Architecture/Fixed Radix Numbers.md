A way of representing unsigned integers as a sequence of weighted and positional digits. 
$$X_v=\sum\limits_{i=0}^{n-1}x_ir^i$$
- Where $r$ is fixed and determines the "base-$n$" system
- When $r=10$ this is just decimal
- When $r=2$ this is binary